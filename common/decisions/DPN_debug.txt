DPN_debug = {
    DPN_change_portrait_0_debug = {
        icon = GFX_decision_generic_propaganda

        allowed = { tag = DPN }
        visible = { tag = DPN is_debug = yes }

        cost = 0

        complete_effect = {
            set_portraits = {
                character = DPN_Dippins
                civilian = { large ="GFX_portrait_DPN_Dippins" }
            }
        }

        ai_will_do = { factor = 0 }
    }

    DPN_change_portrait_1_debug = {
        icon = GFX_decision_generic_propaganda

        allowed = { tag = DPN }
        visible = { tag = DPN is_debug = yes }

        cost = 0

        complete_effect = {
            set_portraits = {
                character = DPN_Dippins
                civilian = { large ="GFX_portrait_DPN_Dippins_alt_1" }
            }
        }

        ai_will_do = { factor = 0 }
    }
    DPN_change_portrait_2_debug = {
        icon = GFX_decision_generic_propaganda

        allowed = { tag = DPN }
        visible = { tag = DPN is_debug = yes }

        cost = 0

        complete_effect = {
            set_portraits = {
                character = DPN_Dippins
                civilian = { large ="GFX_portrait_DPN_Dippins_alt_2" }
            }
        }

        ai_will_do = { factor = 0 }
    }
    DPN_change_portrait_3_debug = {
        icon = GFX_decision_generic_propaganda

        allowed = { tag = DPN }
        visible = { tag = DPN is_debug = yes }

        cost = 0

        complete_effect = {
            set_portraits = {
                character = DPN_Dippins
                civilian = { large ="GFX_portrait_DPN_Dippins_alt_3" }
            }
        }

        ai_will_do = { factor = 0 }
    }
    DPN_change_portrait_4_debug = {
        icon = GFX_decision_generic_propaganda

        allowed = { tag = DPN }
        visible = { tag = DPN is_debug = yes }

        cost = 0

        complete_effect = {
            set_portraits = {
                character = DPN_Dippins
                civilian = { large ="GFX_portrait_DPN_Dippins_alt_4" }
            }
        }

        ai_will_do = { factor = 0 }
    }
}
