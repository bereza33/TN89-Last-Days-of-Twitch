add_namespace = script

country_event = {
	id = script.1
	title = annex_or_war.0.t
	desc = annex_or_war.0.d
	picture = GFX_report_event_generic_diplomacy

	major = no
	is_triggered_only = yes

	# ROOT — страна, которой пришёл ультиматум
	# FROM — страна, которая требует

	option = {
		name = annex_or_war.0.a # Сопротивляться (война)
        
		hidden_effect = {
			FROM = {
				declare_war_on = {
					target = ROOT
					type = annex_everything
				}
			}
		}
	}

	option = {
		name = annex_or_war.0.b # Подчиниться (аннексия)

		hidden_effect = {
			FROM = {
				annex_country = {
					target = ROOT
					transfer_troops = yes
				}
			}
		}
	}
}
country_event = {
	id = script.2
	immediate ={
		create_pie_chart = yes
	}
	title = CULTURE.1776.t
	desc = CULTURE.1776.d
    fire_only_once = yes
	is_triggered_only = yes
	hidden = yes
}
